{
  "name": "agent-runtime",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "pnpm --filter agent-runtime... build",
    "test": "vitest run",
    "dev": "tsx src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@repo/security": "workspace:*",
    "@repo/shared-schemas": "workspace:*",
    "@repo/supabase-client": "workspace:*",
    "@supabase/supabase-js": "^2.48.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@repo/config-typescript": "workspace:*",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
