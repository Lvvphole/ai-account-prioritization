# Supabase Edge Functions

Reserved for Supabase Edge Functions (Deno). None are required by the current
sprints — the deterministic runtime lives in `apps/agent-runtime` and privileged
writes use the service-role client there.

Add functions here as `supabase/functions/<name>/index.ts` and deploy with
`supabase functions deploy <name>`.
